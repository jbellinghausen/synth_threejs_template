{
  "name": "synth-threejs-template",
  "version": "0.1.0",
  "private": true,
  "description": "Template for musical toys on the synth module: CV/gate over WebSocket, a 12-slot UI, tracker, wiring card and a three.js runtime",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "node --test 'test/**/*.test.js'"
  },
  "dependencies": {
    "synth-module-client": "github:jbellinghausen/synth_module_daemon#v0.1.0",
    "three": "^0.186.1"
  },
  "devDependencies": {
    "vite": "^8.3.1"
  }
}
